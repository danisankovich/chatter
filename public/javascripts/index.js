import React, {Component, PropTypes} from 'react';
import ReactDOM from 'react-dom';

import App from './components/App.jsx';

//entrypoint for the app
ReactDOM.render(
  <App />, document.getElementById('entryPoint')
)
